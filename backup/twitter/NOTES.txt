bot.py finds tweets & extracts the text & username as it should.

Bugs:
New lines within a single tweet (as is quite common with arabic language tweets) confuse the
history logging & cause it to spam those users.
